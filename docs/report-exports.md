# Manager report exports

Manager report pages expose PDF and Excel downloads through dedicated export routes. PDF files use a formal report template instead of printing the dashboard page.

After deployment, run `php artisan migrate`, `npm run build`, and `php artisan test`.
